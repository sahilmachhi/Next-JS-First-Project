/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'hoeegskedecawtnvtvok.supabase.co',], // Replace with your domains
    },
};

export default nextConfig;
